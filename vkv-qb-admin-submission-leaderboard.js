import './vkv-qb-admin-pending-age.js?v=20260830-1';
import './vkv-qb-admin-coordinator-report.js?v=20260831-pending-bands-2';
import './vkv-qb-admin-verified-contribution.js?v=20260831-principal-context-3';
import{getApps,getApp}from'https://www.gstatic.com/firebasejs/12.17.1/firebase-app.js';
import{getFirestore,doc,getDoc,collection,query,where,getCountFromServer}from'https://www.gstatic.com/firebasejs/12.17.1/firebase-firestore.js';
const wait=ms=>new Promise(r=>setTimeout(r,ms)),norm=s=>String(s||'').trim().toLowerCase();
function esc(s){return String(s??'').replace(/[&<>"']/g,c=>({'&':'&amp;','<':'&lt;','>':'&gt;','"':'&quot;',"'":'&#39;'}[c]))}
function syncFilters(){const s=document.getElementById('sLSubject'),c=document.getElementById('sLClass'),sourceS=document.getElementById('lSubject'),sourceC=document.getElementById('lClass');if(s&&sourceS){const old=s.value;s.innerHTML=sourceS.innerHTML;if([...s.options].some(o=>o.value===old))s.value=old}if(c&&sourceC){const old=c.value;c.innerHTML=sourceC.innerHTML;if([...c.options].some(o=>o.value===old))c.value=old}}
async function masterTeachers(db){const s=await getDoc(doc(db,'master','current')),raw=s.exists()?s.data()||{}:{},m=raw.data&&typeof raw.data==='object'?{...raw,...raw.data}:raw,map=m.teacherEmailMap||{};return(m.teachers||[]).filter(t=>t&&t.active!==false&&!t.nonTeaching).map(t=>({code:String(t.code||t.shortCode||t.teacherShortCode||t.timetableCode||'').trim(),name:String(t.name||t.fullName||t.code||'Teacher').trim(),email:norm(map[t.code]||t.email||t.gmail||t.googleEmail||t.google_email||'')})).filter(t=>t.code||t.email||t.name).sort((a,b)=>a.name.localeCompare(b.name))}
function buildQ(db,field,value,statuses,sub,cls){const p=[collection(db,'qbQuestions'),where(field,'==',value)];p.push(Array.isArray(statuses)?where('status','in',statuses):where('status','==',statuses));if(sub)p.push(where('subject','==',sub));if(cls)p.push(where('className','==',cls));return query(...p)}
async function countsBy(db,field,value,sub,cls){const[raw,pending]=await Promise.all([getCountFromServer(buildQ(db,field,value,['submitted','approved','returned'],sub,cls)),getCountFromServer(buildQ(db,field,value,'submitted',sub,cls))]);return{raw:raw.data().count,pending:pending.data().count,via:field}}
async function countFor(db,t,sub,cls){const tries=[];if(t.code)tries.push(['teacherCode',t.code]);if(t.email)tries.push(['teacherEmail',t.email]);if(t.name)tries.push(['teacherName',t.name]);let first={raw:0,pending:0,via:'teacherCode'};for(const[field,value]of tries){try{const c=await countsBy(db,field,value,sub,cls);if(!first.raw)first=c;if(c.raw)return c}catch(e){console.warn('Raw submission identity count',field,value,e)}}return first}
async function init(){for(let i=0;i<40&&!document.getElementById('app');i++)await wait(100);const app=document.getElementById('app');if(!app)return;for(let i=0;i<40&&!getApps().length;i++)await wait(100);if(!getApps().length)return;const db=getFirestore(getApp()),tabs=document.querySelector('.tabs');if(!tabs||document.getElementById('submissionLeaderboard'))return;
const btn=document.createElement('button');btn.dataset.panel='submissionLeaderboard';btn.textContent='📈 Raw Submissions';tabs.insertBefore(btn,tabs.querySelector('[data-panel="questions"]'));
const sec=document.createElement('section');sec.id='submissionLeaderboard';sec.className='card panel';sec.innerHTML=`<h2>📈 Raw Question Submission Leaderboard</h2><div class="tip">Ranks teachers by questions submitted into the academic workflow, before considering whether they were later verified or returned. Drafts are excluded. Aggregate counts are used; question documents are not downloaded.</div><div class="grid2" style="margin-top:10px"><div><label>Subject</label><select id="sLSubject"><option value="">All</option></select></div><div><label>Class</label><select id="sLClass"><option value="">All</option></select></div></div><div class="actions"><button id="loadSubmissionLeaderboard" class="primary">Calculate Raw Leaderboard</button><button id="exportRawLeaderboard">Export CSV</button></div><div id="submissionLeaderList"><div class="empty">Click “Calculate Raw Leaderboard”. Nothing is counted on page startup.</div></div>`;document.querySelector('main .wrap #app')?.appendChild(sec);syncFilters();
btn.onclick=()=>{syncFilters();document.querySelectorAll('.tabs button').forEach(x=>x.classList.toggle('active',x===btn));document.querySelectorAll('.panel').forEach(x=>x.classList.toggle('active',x===sec))};let last=[];
document.getElementById('loadSubmissionLeaderboard').onclick=async()=>{const button=document.getElementById('loadSubmissionLeaderboard'),out=document.getElementById('submissionLeaderList'),sub=document.getElementById('sLSubject').value,cls=document.getElementById('sLClass').value;button.disabled=true;const old=button.textContent;button.textContent='Calculating…';out.innerHTML='<div class="empty">Reading the teacher list and calculating aggregate submission counts…</div>';try{const teachers=await masterTeachers(db),rows=[];for(const t of teachers){const c=await countFor(db,t,sub,cls);if(c.raw)rows.push({...t,...c})}rows.sort((a,b)=>b.raw-a.raw||b.pending-a.pending||a.name.localeCompare(b.name));last=rows;const total=rows.reduce((n,x)=>n+x.raw,0),pending=rows.reduce((n,x)=>n+x.pending,0);out.innerHTML=rows.length?`<div class="tip" style="margin:10px 0"><b>${rows.length}</b> contributing teacher(s) · <b>${total}</b> raw submission(s) · <b>${pending}</b> currently awaiting verification.</div>${rows.map((x,i)=>`<div class="leader"><div class="rank">${i===0?'🥇':i===1?'🥈':i===2?'🥉':'#'+(i+1)}</div><div class="grow"><b>${esc(x.name)}</b><div class="small">${esc(x.code)} · Raw submitted: <b>${x.raw}</b> · Awaiting verification: <b>${x.pending}</b>${x.via!=='teacherCode'?` · matched by ${esc(x.via.replace('teacher','teacher '))}`:''}</div></div></div>`).join('')}`:'<div class="empty">No submitted questions were matched to active teachers for this selection. If historical imports are still unmatched, use the Historical Teacher Link Repair tool.</div>'}catch(e){out.innerHTML='<div class="empty">Could not calculate raw submissions: '+esc(e.message||e)+'</div>'}finally{button.disabled=false;button.textContent=old}};
document.getElementById('exportRawLeaderboard').onclick=()=>{if(!last.length)return alert('Calculate the Raw Submission Leaderboard first.');const rows=[['Rank','Teacher','Code','Raw Submitted','Awaiting Verification'],...last.map((x,i)=>[i+1,x.name,x.code,x.raw,x.pending])],csv=rows.map(r=>r.map(v=>'"'+String(v??'').replace(/"/g,'""')+'"').join(',')).join('\r\n'),blob=new Blob(['\ufeff'+csv],{type:'text/csv;charset=utf-8'}),url=URL.createObjectURL(blob),a=document.createElement('a');a.href=url;a.download='VKV_QB_Raw_Submission_Leaderboard.csv';a.click();setTimeout(()=>URL.revokeObjectURL(url),1000)};}
init();