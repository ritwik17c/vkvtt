import './vkv-qb-admin-pending-age.js?v=20260830-1';
import './vkv-qb-admin-coordinator-report.js?v=20260831-pending-bands-2';
import './vkv-qb-admin-verified-contribution.js?v=20260831-principal-context-2';
import{getApps,getApp}from'https://www.gstatic.com/firebasejs/12.17.1/firebase-app.js';
import{getFirestore,collection,query,where,getCountFromServer}from'https://www.gstatic.com/firebasejs/12.17.1/firebase-firestore.js';
const wait=ms=>new Promise(r=>setTimeout(r,ms));
function esc(s){return String(s??'').replace(/[&<>"']/g,c=>({'&':'&amp;','<':'&lt;','>':'&gt;','"':'&quot;',"'":'&#39;'}[c]))}
function teachersFromPage(){const e=document.getElementById('aTeacher');if(!e)return[];return[...e.options].map(o=>{const code=String(o.value||'').trim(),text=String(o.textContent||'').trim(),m=text.match(/^(.*?)\s*\(([^()]*)\)\s*$/);return{code,name:(m?.[1]||text||code).trim()}}).filter(x=>x.code)}
function syncFilters(){const s=document.getElementById('sLSubject'),c=document.getElementById('sLClass'),sourceS=document.getElementById('lSubject'),sourceC=document.getElementById('lClass');if(s&&sourceS){const old=s.value;s.innerHTML=sourceS.innerHTML;if([...s.options].some(o=>o.value===old))s.value=old}if(c&&sourceC){const old=c.value;c.innerHTML=sourceC.innerHTML;if([...c.options].some(o=>o.value===old))c.value=old}}
async function countFor(db,t,sub,cls){const base=[collection(db,'qbQuestions'),where('teacherCode','==',t.code)];const filters=[];if(sub)filters.push(where('subject','==',sub));if(cls)filters.push(where('className','==',cls));const allQ=query(...base,where('status','in',['submitted','approved','returned']),...filters),pendingQ=query(...base,where('status','==','submitted'),...filters);const[all,pending]=await Promise.all([getCountFromServer(allQ),getCountFromServer(pendingQ)]);return{raw:all.data().count,pending:pending.data().count}}
async function init(){for(let i=0;i<40&&!document.getElementById('app');i++)await wait(100);const app=document.getElementById('app');if(!app)return;for(let i=0;i<40&&!getApps().length;i++)await wait(100);if(!getApps().length)return;const db=getFirestore(getApp()),tabs=document.querySelector('.tabs');if(!tabs||document.getElementById('submissionLeaderboard'))return;
const btn=document.createElement('button');btn.dataset.panel='submissionLeaderboard';btn.textContent='📈 Raw Submissions';tabs.insertBefore(btn,tabs.querySelector('[data-panel="questions"]'));
const sec=document.createElement('section');sec.id='submissionLeaderboard';sec.className='card panel';sec.innerHTML=`<h2>📈 Raw Question Submission Leaderboard</h2><div class="tip">Counts every non-draft question submitted into the verification workflow. A question remains part of the raw-submission total after it is verified or returned, so contribution scores never fall when coordinators act. Firestore aggregate counts are used; question documents are not downloaded.</div><div class="grid2" style="margin-top:10px"><div><label>Subject</label><select id="sLSubject"><option value="">All</option></select></div><div><label>Class</label><select id="sLClass"><option value="">All</option></select></div></div><div class="actions"><button id="loadSubmissionLeaderboard" class="primary">Calculate Raw Submissions</button></div><div id="submissionLeaderList"><div class="empty">Click “Calculate Raw Submissions”. Nothing is counted on page startup.</div></div>`;document.querySelector('main .wrap #app')?.appendChild(sec);syncFilters();
btn.addEventListener('click',()=>{syncFilters();document.querySelectorAll('.tabs button').forEach(x=>x.classList.toggle('active',x===btn));document.querySelectorAll('.panel').forEach(x=>x.classList.toggle('active',x===sec))});
document.getElementById('loadSubmissionLeaderboard').onclick=async()=>{const button=document.getElementById('loadSubmissionLeaderboard'),out=document.getElementById('submissionLeaderList'),sub=document.getElementById('sLSubject').value,cls=document.getElementById('sLClass').value,teachers=teachersFromPage();if(!teachers.length){out.innerHTML='<div class="empty">Teacher list is not ready yet. Reopen this tab once the Admin page has finished loading.</div>';return}button.disabled=true;const old=button.textContent;button.textContent='Calculating…';out.innerHTML='<div class="empty">Calculating quota-safe aggregate counts…</div>';try{const rows=[];for(const t of teachers){try{const c=await countFor(db,t,sub,cls);if(c.raw)rows.push({...t,...c})}catch(e){console.warn('Raw submission count',t.code,e)}}rows.sort((a,b)=>b.raw-a.raw||b.pending-a.pending||a.name.localeCompare(b.name));out.innerHTML=rows.length?`<div class="small" style="margin:8px 0">Raw = submitted + later verified/returned. Pending = currently awaiting verification.</div>${rows.map((x,i)=>`<div class="leader"><div class="rank">${i===0?'🥇':i===1?'🥈':i===2?'🥉':'#'+(i+1)}</div><div class="grow"><b>${esc(x.name)}</b><div class="small">${esc(x.code)} · Raw submitted: <b>${x.raw}</b> · Awaiting verification: <b>${x.pending}</b></div></div></div>`).join('')}`:'<div class="empty">No submitted questions for this selection.</div>'}catch(e){out.innerHTML='<div class="empty">Could not calculate raw submissions: '+esc(e.message||e)+'</div>'}finally{button.disabled=false;button.textContent=old}};}
init();