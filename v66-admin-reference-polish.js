/* v66.4 admin dashboard reference polish disabled for stability.
   Canonical grouping and Leave Management consolidation are owned by vkv-admin-dashboard-runtime.js.
   Keep this file intentionally inert to avoid duplicate DOM ownership/regressions. */
(function(){'use strict';})();
