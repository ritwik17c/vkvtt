import{getApps,getApp}from'https://www.gstatic.com/firebasejs/12.17.1/firebase-app.js';
import{getFirestore,collection,query,where,getCountFromServer}from'https://www.gstatic.com/firebasejs/12.17.1/firebase-firestore.js';
const wait=ms=>new Promise(r=>setTimeout(r,ms));
const esc=s=>String(s??'').replace(/[&<>"']/g,c=>({'&':'&amp;','<':'&lt;','>':'&gt;','"':'&quot;',"'":'&#39;'}[c]));
async function init(){for(let i=0;i<40&&!document.getElementById('app');i++)await wait(100);const app=document.getElementById('app');if(!app)return;for(let i=0;i<40&&!getApps().length;i++)await wait(100);if(!getApps().length)return;const tabs=document.querySelector('.tabs');if(!tabs||document.getElementById('verifiedContribution'))return;const db=getFirestore(getApp());
const btn=document.createElement('button');btn.dataset.panel='verifiedContribution';btn.textContent='🏅 Verified Contribution';tabs.insertBefore(btn,tabs.querySelector('[data-panel="questions"]'));
const sec=document.createElement('section');sec.id='verifiedContribution';sec.className='card panel';sec.innerHTML=`<h2>🏅 Verified Contribution Leaderboard</h2><div class="tip">Academic workflow view: ranks teachers by verified questions and also shows verification rate. This is separate from the Submission/Quantity leaderboard and is <b>not</b> a quality score.</div><div class="grid2" style="margin-top:10px"><div><label>Subject</label><select id="vCSubject"><option value="">All</option></select></div><div><label>Class</label><select id="vCClass"><option value="">All</option></select></div></div><div class="actions"><button id="loadVerifiedContribution" class="primary">Calculate Verified Contribution</button></div><div id="verifiedContributionList"><div class="empty">Click “Calculate Verified Contribution”.</div></div>`;document.querySelector('main .wrap #app')?.appendChild(sec);
const ss=document.getElementById('lSubject'),sc=document.getElementById('lClass');if(ss)document.getElementById('vCSubject').innerHTML=ss.innerHTML;if(sc)document.getElementById('vCClass').innerHTML=sc.innerHTML;
btn.onclick=()=>{document.querySelectorAll('.tabs button').forEach(x=>x.classList.toggle('active',x===btn));document.querySelectorAll('.panel').forEach(x=>x.classList.toggle('active',x===sec))};
document.getElementById('loadVerifiedContribution').onclick=async()=>{const out=document.getElementById('verifiedContributionList'),sub=document.getElementById('vCSubject').value,cls=document.getElementById('vCClass').value;out.innerHTML='<div class="empty">Calculating verified contributions…</div>';let master={};try{const{doc,getDoc}=await import('https://www.gstatic.com/firebasejs/12.17.1/firebase-firestore.js');const s=await getDoc(doc(db,'master','current'));master=s.exists()?(s.data().data||s.data()):{}}catch{}const teachers=(master.teachers||[]).filter(t=>t&&t.active!==false&&!t.nonTeaching),rows=[];for(const t of teachers){const base=[collection(db,'qbQuestions'),where('teacherCode','==',t.code)];const filters=[];if(sub)filters.push(where('subject','==',sub));if(cls)filters.push(where('className','==',cls));try{const [submitted,verified]=await Promise.all([getCountFromServer(query(...base,where('status','in',['submitted','approved','returned']),...filters)),getCountFromServer(query(...base,where('status','==','approved'),...filters))]);const total=submitted.data().count,ok=verified.data().count;if(total)rows.push({name:t.name||t.code,code:t.code,total,ok,rate:Math.round(ok*1000/total)/10})}catch(e){console.warn('Verified contribution count',t.code,e)}}rows.sort((a,b)=>b.ok-a.ok||b.rate-a.rate||a.name.localeCompare(b.name));out.innerHTML=rows.length?rows.map((x,i)=>`<div class="leader"><div class="rank">${i===0?'🥇':i===1?'🥈':i===2?'🥉':'#'+(i+1)}</div><div class="grow"><b>${esc(x.name)}</b><div class="small">${esc(x.code)} · Verified: <b>${x.ok}</b> of ${x.total} submitted · Verification rate: <b>${x.rate}%</b></div></div></div>`).join(''):'<div class="empty">No submitted questions for this selection.</div>'};}
init();